sudo: required

services:
  - docker
 
before_install:
  - apt-get update
  - apt-get install -y net-tools iputils-ping
  
install:
  - ./install_docker_ovs.sh

script:
  - ./load_image.sh
  - ./start.sh
  - ./test.sh
  - ./stop.sh
